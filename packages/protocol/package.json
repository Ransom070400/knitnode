{
  "name": "@knitnode/protocol",
  "version": "0.1.0",
  "description": "KnitNode shared types + binary wire format for vector KV entries.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0",
    "cbor-x": "^1.6.0"
  }
}
